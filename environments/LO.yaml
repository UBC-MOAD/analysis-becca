# conda environment description file for the analysis_becca/LiveOcean
#
# Create a conda environment in which to run Jupyter Notebooks in this directory with:
#
#   $ conda env create -f LO.yaml
#   $ conda activate LO
#
# To add new packages to the environment,
# add them to the dependencies list below,
# then update the activated environment with:
#
#   (LO)$ conda env update -f LO.yaml

name: LO

channels:
  - conda-forge
  - defaults

dependencies:
  - arrow
  - bottleneck
  - cmocean
  - dask
  - python-dateutil
  - jupyterlab
  - matplotlib
  - netCDF4
  - numpy
  - pandas
  - pip
  - python=3
  - pytz
  - scipy
  - xarray
  - IPython
  - cartopy
  - scikit-learn
  - geopandas
  - gsw

# pip install --user --editable /ocean/rbeutel/MEOPAR/tools/SalishSeaTools
